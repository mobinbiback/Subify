# Subify Changelog

## v2.5.0 — 2026-09-09

نسخهٔ قبلی (v2.4.17) چند مشکل واقعی داشت: امکان قطع‌شدن ترجمه‌های طولانی وسط کار، ناهماهنگی بین توگل زیرنویس در Popup و پخش‌کننده، و از همه مهم‌تر — دسترسی افزونه به «همهٔ سایت‌های اینترنت» به‌جای فقط سایت‌های موردنیاز. در این نسخه مسیر Custom API Provider از پایه بازطراحی شد: حالا افزونه فقط برای همون یک دامنه‌ای که خودت به‌عنوان سرویس سفارشی معرفی می‌کنی درخواست اجازه می‌کنه، نه دسترسی از‌پیش به کل اینترنت.

### قابلیت‌های جدید
- **داشبورد آمار مصرف و برآورد هزینه** (Options): کاراکتر ترجمه‌شدهٔ زیرنویس، ثانیهٔ دوبله، تعداد جلسه و export، به‌تفکیک هر provider. هزینه با نرخی که خودت وارد می‌کنی تخمین زده می‌شود — قیمتی از پیش حدس زده نشده، چون نرخ‌ها بین سرویس‌ها/مدل‌ها فرق دارد و مدام تغییر می‌کند.
- **خروجی صوت MP3**: علاوه بر WAV، حالا می‌شود دوبله را با MP3 (~۶۴kbps، حدود ۴ برابر کوچک‌تر) دانلود کرد. رمزگذاری آفلاین و با کتابخانهٔ lamejs انجام می‌شود (نه real-time، برخلاف MediaRecorder/Opus).
- **پشتیبانی از پلتفرم‌های جدید**: Vimeo، آپارات و Coursera علاوه بر یوتیوب — با استفاده از Web API استاندارد `TextTrack` که از قبل در کد وجود داشت ولی فقط برای یوتیوب فعال بود.
- **پاک‌کردن کامل داده‌ها + سند حریم خصوصی** (`PRIVACY.md`, Options): یک دکمه برای حذف کامل کلیدهای API، تنظیمات، آمار مصرف، دیکشنری و حافظهٔ ترجمهٔ ذخیره‌شده — هم برای تب‌های باز، هم (با یک پرچم pending) برای تب‌هایی که موقع کلیک بسته بودند.
- **صفحهٔ «چه چیزی تغییر کرد»**: از این نسخه به بعد، بعد از هر آپدیت یک صفحهٔ خلاصه‌ی تغییرات باز می‌شود (`whatsnew.html`).

### رفع اشکال
- سقف `maxOutputTokens` ترجمهٔ Gemini با اندازهٔ batch (۲۴ خط) هماهنگ نبود و می‌توانست JSON خروجی را وسط کار قطع کند — اصلاح شد.
- فعال‌شدن خودکار زیرنویس (هنگام refresh caption) در storage ذخیره نمی‌شد و توگل Popup را sync نمی‌کرد — اصلاح شد.
- کد امضای مبهم و تکراری (`__sby_p`) که در ۱۰ فایل کپی شده بود حذف شد.

### امنیت
- `host_permissions` از `https://*/*` و `http://*/*` (دسترسی به کل اینترنت) به فقط دامنه‌های واقعاً موردنیاز محدود شد.
- Custom API Provider حالا پیش از هر استفاده، طبق الگوی رسمی MV3 (`optional_host_permissions` + `browser.permissions.request`)، فقط برای همان یک دامنه اجازه می‌گیرد.

---

## v2.4.17 و قبل‌تر
تاریخچهٔ کامل‌تر این نسخه‌ها مستند نشده بود؛ این فایل از v2.5.0 به بعد نگه‌داری می‌شود.
