# Subify Lite Changelog

## v1.0.0 — اولین نسخه

Subify Lite یه خط محصول جدا از Subify اصلیه — همون موتور ترجمهٔ زیرنویس، بدون دوبلهٔ صوتی. علتش سادست: دوبله به `chrome.tabCapture`/`chrome.offscreen` نیاز داره، و نه Firefox (دسکتاپ و اندروید) نه Safari (iOS) این APIها رو پیاده‌سازی نکردن. Subify اصلی (با دوبله) همچنان مخصوص Chrome/دسکتاپه؛ این نسخه برای جاهاییه که دوبله اصلاً امکان‌پذیر نیست.

### چیزی که هست
- ترجمهٔ زیرنویس زمان‌بندی‌شده روی یوتیوب، ویمیو، آپارات و Coursera
- ترجمه پیش‌دستانه با همگام‌سازی دقیق
- چند ارائه‌دهنده: Gemini، OpenRouter، OpenAI، یا هر Custom API
- خروجی زیرنویس SRT/VTT/TXT
- استایل‌دهی کامل زیرنویس با پیش‌نمایش زنده
- یادگیری لغت با سیستم Leitner
- داشبورد آمار مصرف و برآورد هزینه (با نرخ اختصاصی خودت)
- پاک‌کردن کامل داده‌ها + سند حریم خصوصی

### چیزی که نیست (و چرا)
- دوبلهٔ صوتی، خروجی WAV/MP3، زیرنویس زندهٔ STT — همه به گرفتن صدای تب نیاز دارن که خارج از Chrome در دسترس نیست.

### قبل از انتشار
- هنوز روی Firefox واقعی (دسکتاپ و اندروید) تست نشده — قبل از فرستادن به AMO حتماً خودت امتحان کن.
- `browser_specific_settings.gecko.id` توی manifest.json یه مقدار پیشنهادیه (`subify-lite@mobinbibak.dev`)؛ همون رو نگه دار یا با شناسهٔ دلخواه خودت عوضش کن، فقط بعد از اولین انتشار در AMO دیگه عوضش نکن.
